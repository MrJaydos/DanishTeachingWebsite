# Dependencies (installed fresh inside the image)
node_modules
*/node_modules

# Build artifacts
client/dist
dist

# Capacitor native projects (built manually via Xcode/Android Studio, not
# part of the web Docker image)
client/ios
client/android
client/assets

# Env & secrets
.env
.env.local
*/.env
client/.env.capacitor

# Git & CI
.git
.github
.gitignore

# Docs / editor
*.md
.vscode
.idea
.DS_Store

# Local compose data
pgdata
